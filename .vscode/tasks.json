{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "cmake",
      "label": "CMake: build DvmEngine",
      "command": "build",
      "targets": [
        "DvmEngine"
      ],
      "preset": "${command:cmake.activeBuildPresetName}",
      "group": "build",
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}/build"
      },
      "detail": "CMake template build task"
    }
  ]
}